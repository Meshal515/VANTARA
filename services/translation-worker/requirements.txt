# VANTARA translation worker — production dependencies.
# الرسم العربي من Pillow/RAQM (الويل الرسمي يحمل HarfBuzz + FriBidi + libraqm).
# كل نماذج الرؤية وOCR تعمل بـonnxruntime + OpenCV DNN: لا torch ولا paddle في الإنتاج.
# النسخ هي ما جرى عليه القياس على الصفحات الحقيقية (2026-09-24).

Pillow==12.3.0
numpy==2.4.6
opencv-python-headless==5.0.0.93
onnxruntime==1.30.0

# الخادم والعميل
fastapi==0.141.1
uvicorn==0.53.0
httpx==0.28.1
pydantic==2.13.5
