GSAP 3.15.0 — https://gsap.com
Copyright 2026, GreenSock. All rights reserved.

Distributed under the GSAP Standard "No Charge" License:
https://gsap.com/standard-license

`gsap.esm.js` is the `gsap@3.15.0` npm package's ES module entry (`index.js`:
gsap core + CSSPlugin), bundled into one file and minified with esbuild
(`esbuild index.js --bundle --format=esm --minify --legal-comments=inline`).
The GSAP code itself is unmodified.
